#dashboard_top
  #dash_left

    %h1== Hi, #{current_user.name}

    = avatar_url(current_user)

    - if current_user.first_login?
      = render 'first_login'
    -else
      = render 'stats'

  #dash_right
    = render 'shared/pie_chart'

= render 'updates'

%br
%h3 Your API key
%code#api_key= @current_user.api_key







