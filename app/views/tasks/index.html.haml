%h1 Listing active tasks

%p A place to organise your daily tasks

%p= paginate @tasks

%table
  %tr
    %th Title
    %th Status
    %th Project
    %th
    %th

  - @tasks.each do |task|
    %tr
      %td= task.title
      %td= task.status
      %td= task.project_title
      %td= link_to 'Edit', edit_task_path(task)
      %td= link_to 'Destroy', task, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Task', new_task_path, :class => 'button'
