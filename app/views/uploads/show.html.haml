%h2= @upload.title
%p= @upload.category
%p=@upload.file_content_type

- if @upload.is_public?
  %p== Public URL /uploads/share/#{@upload.id}

- if @upload.image?
  %img{:src => @upload.file, :alt => @upload.title}

%br 

= link_to 'Edit', edit_upload_path(@upload)
\|
= link_to 'Back', uploads_path
