%h1 Dashboard
%p This is your dashboard

= render 'shared/stats'

%h2 Recent activity

- @projects.each do |project|
  %div.update== #{@user.first_name} created the project #{project.title} on #{pretty_date project.created_at}



